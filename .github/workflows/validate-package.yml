name: Validate package

on:
  workflow_call:
    inputs:
      package_version:
        required: false
        type: string
        default: 0.0.0-alpha.package-tests

env:
  PIXELY_PACKAGE_VERSION: ${{ inputs.package_version }}
  PIXELY_PACKAGE_ARTIFACT: Pixely-${{ inputs.package_version }}

jobs:
  package:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.0.x'

      - name: Pack validated commit
        env:
          REPOSITORY_COMMIT: ${{ github.sha }}
          REPOSITORY_BRANCH: ${{ github.ref }}
        run: >-
          dotnet pack packaging/Pixely/Pixely.Package.csproj
          --configuration Release
          --output artifacts/packages
          --property:PackageVersion=$PIXELY_PACKAGE_VERSION
          --property:Version=$PIXELY_PACKAGE_VERSION
          --property:RepositoryCommit=$REPOSITORY_COMMIT
          --property:RepositoryBranch=$REPOSITORY_BRANCH
          --property:ContinuousIntegrationBuild=true
          -p:TreatWarningsAsErrors=true

      - name: Upload package artifacts
        uses: actions/upload-artifact@v7
        with:
          name: ${{ env.PIXELY_PACKAGE_ARTIFACT }}
          path: |
            artifacts/packages/Pixely.${{ env.PIXELY_PACKAGE_VERSION }}.nupkg
            artifacts/packages/Pixely.${{ env.PIXELY_PACKAGE_VERSION }}.snupkg
          if-no-files-found: error
          overwrite: true
          retention-days: 1

  package-consumer:
    needs: package
    timeout-minutes: 30
    strategy:
      matrix:
        os:
          - ubuntu-24.04
          - ubuntu-24.04-arm
          - windows-latest
          - macos-15-intel
          - macos-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.0.x'

      - name: Download package artifacts
        uses: actions/download-artifact@v8
        with:
          name: ${{ env.PIXELY_PACKAGE_ARTIFACT }}
          path: artifacts/package-under-test

      - name: Build package integration tests
        run: dotnet build tests/Pixely.Package.Tests/Pixely.Package.Tests.csproj -p:TreatWarningsAsErrors=true

      - name: Test local package consumers
        env:
          PIXELY_PACKAGE_DIRECTORY: ${{ github.workspace }}/artifacts/package-under-test
        run: dotnet test tests/Pixely.Package.Tests/Pixely.Package.Tests.csproj --no-build --filter 'TestCategory=PackageIntegration' -p:TreatWarningsAsErrors=true
